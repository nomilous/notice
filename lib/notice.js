// Generated by CoffeeScript 1.4.0
var configure, notice, notify;

configure = require('./notice/configure');

notify = require('./notice/notify');

notice = notify.send;

notice.configure = configure;

notice.event = {
  good: function() {
    return notify.send(notify.format({
      type: 'event',
      tenor: 'good'
    }, arguments));
  },
  normal: function() {
    return notify.send(notify.format({
      type: 'event',
      tenor: 'normal'
    }, arguments));
  },
  bad: function() {
    return notify.send(notify.format({
      type: 'event',
      tenor: 'bad'
    }, arguments));
  }
};

notice.info = {
  good: function() {
    return notify.send(notify.format({
      type: 'info',
      tenor: 'good'
    }, arguments));
  },
  normal: function() {
    return notify.send(notify.format({
      type: 'info',
      tenor: 'normal'
    }, arguments));
  },
  bad: function() {
    return notify.send(notify.format({
      type: 'info',
      tenor: 'bad'
    }, arguments));
  }
};

module.exports = notice;
