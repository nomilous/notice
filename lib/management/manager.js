// Generated by CoffeeScript 1.6.3
var authenticator, testable;

authenticator = require('./authenticator').authenticator;

testable = void 0;

module.exports._manager = function() {
  return testable;
};

module.exports.manager = function(config) {
  var authenticate, listen, local, server;
  if (config == null) {
    config = {};
  }
  listen = config.manage.listen;
  authenticate = authenticator(config);
  server = (listen.cert != null) && (listen.key != null) ? require('https').createServer() : void 0;
  server || (server = require('http').createServer());
  return testable = local = {};
};
