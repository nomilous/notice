// Generated by CoffeeScript 1.6.3
var authenticator, missingConfig, testable;

authenticator = require('./authenticator').authenticator;

missingConfig = require('../notice/errors').missingConfig;

testable = void 0;

module.exports._manager = function() {
  return testable;
};

module.exports.manager = function(config) {
  var authenticate, listen, local, server;
  if (config == null) {
    config = {};
  }
  listen = config.manage.listen;
  authenticate = authenticator(config);
  if (typeof listen.port !== 'number') {
    throw missingConfig('config.manage.port', 'manage');
  }
  testable = local = {};
  server = (function() {
    if ((listen.cert != null) && (listen.key != null)) {
      try {
        return require('https').createServer();
      } catch (_error) {}
    }
  })();
  server || (server = require('http').createServer(function() {}));
  return server.listen(listen.port);
};
