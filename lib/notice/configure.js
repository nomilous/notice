// Generated by CoffeeScript 1.4.0
var config, configure, defaultMessenger, defaultSource, localMessenger;

config = {};

defaultSource = require('./default_source');

defaultMessenger = require('./default_messenger');

localMessenger = require('./local_messenger');

module.exports = configure = function(opts) {
  if (opts == null) {
    opts = {};
  }
  config.source = opts.source || defaultSource();
  return config.messenger = localMessenger() || opts.messenger || defaultMessenger;
};

Object.defineProperty(configure, 'config', {
  get: function() {
    return config;
  }
});
